# Database
DATABASE_URL="file:./dev.db"

# Authentication (for admin panel)
NEXTAUTH_SECRET="your-nextauth-secret-here"  # Generate using: openssl rand -base64 32
NEXTAUTH_URL="http://localhost:3000"

# JWT Secret (REQUIRED for admin authentication)
JWT_SECRET=""  # REQUIRED: Generate using: openssl rand -base64 32

# Admin credentials (CHANGE THESE FOR PRODUCTION!)
ADMIN_EMAIL="admin@mohammadkfelati.com"
ADMIN_PASSWORD=""  # REQUIRED: Set a strong password (minimum 12 characters)

# Base URL (for production deployment)
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# Security Settings
NODE_ENV="development"  # Change to "production" for live deployment

# Optional: Rate Limiting Redis URL (for production)
# REDIS_URL="redis://localhost:6379"
